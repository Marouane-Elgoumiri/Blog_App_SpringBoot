server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./blog_app;AUTO_SERVER=TRUE;
    username: test
    password: test
  h2:
    console.enabled: true
  jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: create # in production